# hello-world-express with clojurescript

### Commands

```bash
# compile
lein cljsbuild once

# watch for changes
lein cljsbuild auto

# run nodemon for auto-reload
node_modules/.bin/nodemon out/server.js
```
